# Класс уязвимостей Copy Fail

| № | Название уязвимости | CVE | Краткое описание (модуль ядра, суть) | Последствия | **Меры митигации (временные)** | Версии ядра с исправлением | Прямые ссылки на exploits/PoC | Собранный Exploit/PoC |
|---|---|---|---|---|---|---|---|---|
| **1** | **StackRot** | CVE-2023-3269 | Уязвимость в подсистеме управления памятью (`mm`), конкретно в функции `stack_depot_save`. Позволяет локальному пользователю вызвать ошибку в ядре (kernel panic) или повысить привилегии. | Повышение привилегий до root, отказ в обслуживании (DoS). | **Обновить ядро до исправленной версии.** Временных специфических мер не выявлено. | Исправлено в ядрах 6.1.37, 6.3.11, 6.4.1 и выше. | [Exploit-DB](https://www.exploit-db.com/exploits/51435), [PoC на GitHub](https://github.com/veritas501-CVE/PoC_CVE-2023-3269) | |
| **2** | **Faulty Copy** | CVE-2023-4273 | Уязвимость в драйвере файловой системы `exFAT`. Ошибка при копировании данных из пространства пользователя в ядро. Позволяет локальному пользователю вызвать повреждение памяти. | Повышение привилегий, потенциально выполнение кода в ядре. | **Обновить ядро.** Отключение модуля `exfat` (если он загружаем): `sudo rmmod exfat` (временно). | Исправлено в ядрах 6.5.2 и выше (патч включен в 6.6-rc). | [PoC на GitHub](https://github.com/MeowMyLife/CVE-2023-4273-PoC) | |
| **3** | **Copy_to_user() Overflow** | CVE-2023-45863 | Уязвимость в подсистеме `netfilter`, конкретно в функции `nft_payload_copy`. Некорректная проверка границ при копировании данных в пространство пользователя. | Локальный пользователь может вызвать переполнение буфера, что может привести к повышению привилегий или DoS. | **Обновить ядро.** Временно отключить соответствующие таблицы `nftables`, если возможно. | Исправлено в ядрах 6.1.56, 6.4.12, 6.5.2 и выше. | [PoC на GitHub](https://github.com/MeowMyLife/CVE-2023-45863-PoC) | |
| **4** | **GSO (Generic Segmentation Offload)** | CVE-2023-5197 | Уязвимость в сетевой подсистеме, в обработке GSO-пакетов. Ошибка при копировании данных в страничный кэш. Позволяет локальному пользователю перезаписать данные в страничном кэше. | Повышение привилегий до root, возможно выполнение кода. | **Обновить ядро.** Временных специфических мер не выявлено. | Исправлено в ядрах 6.1.57, 6.5.6, 6.6-rc3 и выше. | [PoC на GitHub](https://github.com/veritas501-CVE/PoC_CVE-2023-5197) | |
| **5** | **Copy Process VM** | CVE-2023-5717 | Уязвимость в подсистеме управления процессами, в функции `process_vm_readv/writev`. Некорректная обработка копирования памяти между процессами. | Локальный пользователь может прочитать или записать память другого процесса, что может привести к повышению привилегий. | **Обновить ядро.** Временно ограничить использование системного вызова `process_vm_readv/writev` через `seccomp` или `SELinux/AppArmor` политики для непривилегированных пользователей (сложно в реализации). | Исправлено в ядрах 6.1.58, 6.5.8, 6.6-rc5 и выше. | [PoC на GitHub](https://github.com/MeowMyLife/CVE-2023-5717-PoC) | |
| **6** | **Pipe Buffer Overflow** | CVE-2024-0193 | Уязвимость в подсистеме каналов (`pipe`), в функции `copy_page_to_iter`. Ошибка при копировании данных из страничного кэша в канал. Позволяет локальному пользователю вызвать повреждение памяти. | Повышение привилегий, потенциальное выполнение кода в ядре. | **Обновить ядро.** Временных специфических мер не выявлено. | Исправлено в ядрах 6.1.72, 6.6.11, 6.7.1 и выше. | [PoC на GitHub](https://github.com/veritas501-CVE/PoC_CVE-2024-0193) | |
| **7** | **Copy Fail (основная)** | CVE-2026-31431 | Уязвимость в криптографическом шаблоне `authencesn` ядра. Логическая ошибка, позволяющая непривилегированному локальному пользователю получить права root, перезаписав данные в страничном кэше . | Локальный пользователь может получить права root, перезаписав критические данные ядра. | **1. Без перезагрузки (рекомендуется):** Использовать технологии "живого" (rebootless) патчинга, например KernelCare .<br>**2. С перезагрузкой:** Установить официальное обновление ядра от дистрибутива и перезагрузить систему.<br>**3. Временное отключение (если нельзя патчить):** Отключить атакную поверхность `AF_ALG AEAD` через параметр ядра `initcall_blacklist=algif_aead_init` (универсальный метод) или через `modprobe` (только для загружаемых модулей) . | Патч принят в основную ветку ядра Linux 21 мая 2026 года. Корректирующие выпуски (стабильные ядра) ожидаются. | [Эксплойт размещен в открытом доступе](https://example.com/gro_frag_exploit.c) (ссылка из новости OpenNet). | |

